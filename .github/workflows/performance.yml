name: Performance Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    - cron: '0 2 * * *'

jobs:
  performance-smoke:
    name: Smoke Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build
        env:
          NEXT_PUBLIC_STACKS_NETWORK: testnet

      - name: Start application
        run: npm start &
        env:
          NODE_ENV: production
          PORT: 3000

      - name: Wait for application
        run: npx wait-on http://localhost:3000 -t 60000

      - name: Install K6
        run: |
          sudo gpg -k
          sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update
          sudo apt-get install k6

      - name: Run smoke test
        run: k6 run tests/performance/smoke-test.js --out json=smoke-results.json
        env:
          BASE_URL: http://localhost:3000

      - name: Display test summary
        if: always()
        run: node tests/performance/summary.js smoke-results.json

      - name: Check performance baseline
        run: node tests/performance/compare-baseline.js smoke-results.json
        continue-on-error: true

      - name: Generate performance report
        if: always()
        run: node tests/performance/generate-report.js smoke-results.json smoke-report.html

      - name: Upload performance report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: smoke-performance-report
          path: smoke-report.html

      - name: Upload smoke test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: smoke-test-results
          path: smoke-results.json

  performance-load:
    name: Load Test
    runs-on: ubuntu-latest
    needs: performance-smoke
    if: github.event_name == 'push' || github.event_name == 'schedule'

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build
        env:
          NEXT_PUBLIC_STACKS_NETWORK: testnet

      - name: Start application
        run: npm start &
        env:
          NODE_ENV: production
          PORT: 3000

      - name: Wait for application
        run: npx wait-on http://localhost:3000 -t 60000

      - name: Install K6
        run: |
          sudo gpg -k
          sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update
          sudo apt-get install k6

      - name: Run load test
        run: k6 run tests/performance/load-test.js --out json=load-results.json
        env:
          BASE_URL: http://localhost:3000

      - name: Display test summary
        if: always()
        run: node tests/performance/summary.js load-results.json

      - name: Check performance baseline
        run: node tests/performance/compare-baseline.js load-results.json

      - name: Generate performance report
        if: always()
        run: node tests/performance/generate-report.js load-results.json load-report.html

      - name: Upload performance report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: load-performance-report
          path: load-report.html

      - name: Upload load test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: load-test-results
          path: load-results.json
