# Enforce engine requirements declared in package.json
engine-strict=true

# Do not silently ignore peer dependency conflicts
legacy-peer-deps=false

# Reduce noise in install output
fund=false
audit-level=moderate
